1. UX logic: when end of recording, the recording control view should maintain on same screen, instead of closing the window.
2. UX logic: the plus button is not obviouse for creating a new recording.
3. UX logic: The recording and transcription should be able to export/share or directly copiable text from the app.
4. The test is slow, need to be optimized. (currently taking 8minutes)
5. UI bug: when close the recording window then reopen it, the recording animation will move left and right.